numbers = list(range(1,6))
print(numbers)

#using range() to tell Python to skip numbers in a given range
#1st arg specifies sarting point
#2nd arg specifies end point
#3rd arg specifies how many numbers are added to the starting point
even_numbers = list(range(2,11,2))
print(even_numbers)

